# Doucment Stage Artifact

This artifact belongs to a non-standard stage slug. It should appear under "Out of Scope" in the artifacts panel since it doesn't match any known workflow stage.

## Notes

- Used for testing the OutOfScope category
- Verifies that unknown slugs render without color styling
- Confirms the panel handles arbitrary stage names gracefully

> This is a blockquote to test blockquote rendering.

Here is an inline `code snippet` and a list:

- Item one
- Item two
- Item three
